# styles

Reserved for additional global stylesheets (e.g. print styles, email-safe
CSS) that don't belong in `src/app/globals.css`. Empty for now — the design
tokens live in `globals.css` since that's what Next.js loads automatically
via the root layout.
